{
  "name": "aurora-hotel-e2e-tests",
  "version": "1.0.0",
  "description": "Full E2E Playwright tests for Aurora Hotel Plaza",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0"
  }
}
